[profile.default]
src = 'src'
out = 'out'
libs = ['lib']
solc = "0.8.17"

[profile.etherscan]
mainnet = { key = "${ETHERSCAN_API_KEY}", url = "${ETHERSCAN_ENDPOINT_URL}" }

# See more config options https://github.com/foundry-rs/foundry/tree/master/config